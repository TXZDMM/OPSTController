# OPSTController
这是一个用于阻止和恢复其他应用更改文件的默认打开方式，同时尽可能地地占用静默。默认配置了8秒自动拒绝






OTHER
请注意，目录test中放的是一个用于测试保护是否生效的程序是使用了ai编写，所以没有源代码
win10默认基准是test中的.json文件，可以在设置-导入基准中配置
win10默认基准是在虚拟机下全新的win10扩展名项目，不包含任何第三方软件设置
